# -*- coding: utf-8 -*-
"""Deep Learning Yolo Model For Cat Dataset.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/13Y80MwLergCuKI49PhQuCrXLq4qRxXgo
"""

!pip install roboflow

from roboflow import Roboflow
rf = Roboflow(api_key="uWK1njf9ORCYlNRV6AkK")
project = rf.workspace("cao-chflm").project("cat-detector-j6uoz")
version = project.version(11)
dataset = version.download("yolov8")

from roboflow import Roboflow

!pip install ultralytics roboflow

from ultralytics import YOLO

model = YOLO("yolov8n.pt")  # Use "yolov8m.pt" or "yolov8l.pt" for better accuracy

model.train(
    data="/content/CAT-DETECTOR-11/data.yaml",  # Correct path from the dataset
    epochs=125,  # Increase if needed
    imgsz=640,  # Image size
    batch=8,  # Adjust based on GPU
    device="cuda"  # Use GPU if available
)

model.eval()

model.predict('/content/th (3).jpg',save=True)

# Load your image
image_path = "/content/th (4).jpg"
results = model(image_path)  # Run detection

# Show results
for result in results:
    result.show()

# Load your image
image_path = "/content/th (3).jpg"
results = model(image_path)  # Run detection

# Show results
for result in results:
    result.show()

# Load your image
image_path = "/content/th (5).jpg"
results = model(image_path)  # Run detection

# Show results
for result in results:
    result.show()